pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolution {
    repositoryMode.set(RepositoryMode.PREFER_PROJECT)
}
rootProject.name = "WordGuard"
include ':app'
